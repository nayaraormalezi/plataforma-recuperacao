import type { User } from '../types';

export const mockUser: User = {
  id: 'user-001',
  nome: 'Patrícia Mendes',
  matricula: 'CAX123456',
  perfil: 'atendente',
  unidade: 'Central de Atendimento — SP',
};
